# Hash Manifests

The lab generated two SHA-256 manifests on the examiner workstation:

- `MASTER_SHA256.csv`
- `WORKING_SHA256.csv`

The manifests were compared by relative file path and SHA-256 value; PowerShell returned no differences.

These CSV files were generated locally and are not included in this prepared package because they were not uploaded into this chat. Before publishing the repository, copy your actual files from:

`C:\DFIR_Lab\Hashes\`

into this `hashes/` directory.

Do not fabricate or manually retype hash values. Upload the original CSV outputs created during the lab.
